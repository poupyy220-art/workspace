# 料號管理中心－出勤表工具整合工作日誌

- 日期：2026-08-31
- 執行者：Codex
- 目標版本：v2.15.0
- 分支：`feature/attendance-tool-v2.15.0`

## 需求與決策

- 將已驗證的出勤表工具整合到料號管理中心，不另建請假資料庫、不串接 HR 系統。
- 保留 Excel 讀取、灰色／週末判斷、異常確認、隨機上下班與時間不重複驗證。
- 月底產生前可逐日設定正常出勤、全天請假、時數假；時數假必須確認實際上下班，避免代替 HR 規則推算。
- 使用者處理紀錄僅存於目前瀏覽器的 localStorage，不上傳 Excel、姓名、員工編號或請假明細。

## 來源與保留

- 整合來源：`出勤表自動填寫工具v10_導覽可操作_20260831.html`
- 正式模組：`modules/attendance-tool/index.html`
- 原始獨立 HTML 保留，未覆蓋或刪除。

## 修改內容

- `index.html`：新增行政工具導覽、首頁卡片、出勤表 iframe 與頁面標題；版本升至 v2.15.0；v2.14.5 移入歷史紀錄。
- `modules/attendance-tool/index.html`：新增內嵌版精簡水平導覽，功能邏輯不變。
- `.github/workflows/deploy.yml`：將出勤模組加入部署觸發路徑及正式資產白名單。
- `README.md`：登記正式模組位置與本機處理原則。
- BOM 頁面、說明與快取版本同步更新為 v2.15.0／2026-08-31，BOM 商業規則未修改。

## 驗證紀錄

- 已完成：主頁、出勤模組與 BOM 模組 JavaScript 語法檢查均通過。
- 已完成：出勤模組 HTML `div` 結構 29／29；既有自動測試 9,223 項全部通過，包含上下班時間不重複與人工時間重複阻擋。
- 已完成：Edge 無頭瀏覽器以 1440×1000 實際開啟內嵌模組，水平導覽、上傳區與規則卡顯示正常。
- 已完成：`git diff --check` 通過。
- 已完成：依正式部署 workflow 建立 17 個發布資產；Cloudflare Wrangler dry-run 通過。
- 尚未完成：BOM Golden Sample 回歸；新複本及舊開發複本均未找到 checklist 所列 Tiny8、Adapter cage、防塵罩與 M90a 測試活頁簿。本次未修改 BOM 商業規則。
- 已完成：正式首頁與出勤模組分別回應 HTTP 200；首頁可讀取 v2.15.0 與「出勤表自動填寫」，模組頁可正常讀取。

## 部署與外部通知

- 使用者已於 2026-08-31 明確同意合併、部署與沿用既有通知流程。
- PR：`#12`，已合併至 `main`。
- 正式 merge commit：`0af35b8c0870e9411d2f4a53a3837bd777dbaa63`。
- GitHub Pages workflow：`33374145513`，成功。
- Cloudflare workflow：`33374146871`，成功。
- 通知 workflow：`33374146840`，成功。
- 正式網站：`https://poupyy220-art.github.io/workspace/`。
- 出勤模組：`https://poupyy220-art.github.io/workspace/modules/attendance-tool/index.html?v=10`。

## v2.15.1 自訂時數介面修正

- 問題：時數假選擇「自訂」後，第三欄僅有 80px，快捷選單與數字欄同列時把數字欄壓縮到幾乎無法操作。
- 修正：時數區最小寬度改為 170px；快捷選單至少 88px；自訂數字欄固定 72px。
- 輸入規則維持 0.5–8 小時、每次 0.5 小時，未修改任何 Excel 填寫、判斷或既有工具設定。
- 已完成：既有出勤工具 9,223 項回歸、JavaScript 語法、版面尺寸斷言與 `git diff --check` 全部通過。
- 待完成：正式部署及線上驗收。

## 本機預覽：其他假別與選填備註

- 其他假別下拉新增：出差、公出、婚假、喪假、產假、自行填寫。
- 選「自行填寫」才展開假別名稱欄位。
- 每筆請假新增「＋ 新增備註（選填）」，預設收合；有填內容時附加在該日所選假別儲存格，不新增或猜測 Excel 欄位。
- 批次套用不複製備註及實際上下班，避免誤套個別資訊。
- 狀態：僅供本機預覽，尚未合併或部署。
