# 料號管理中心－修改與發布必讀清單

> 本檔是每次修改、測試與發布前的必讀文件。開始工作時先完整閱讀；完成工作前逐項核對。不得因為改動很小而省略。

## 1. 工作範圍與資料安全

- [ ] 先確認本次需求、允許修改的範圍及禁止事項。
- [ ] 檢查工作目錄現有變更，只處理本次任務檔案，不覆蓋使用者或其他工作的修改。
- [ ] 不把 BOM、料號、客戶、公司、檔名或 Excel 原始內容傳至伺服器、AI、回饋表單或郵件。
- [ ] 未經使用者確認，不改變 Python baseline 的 Business Rule 結果。

## 2. BOM 固定規則

- [ ] 正式 BOM 頁籤色 `#00B050`（綠色）與 `#92D050`（淡綠色）必須掃描。
- [ ] 其他顏色或無色頁籤原則上保留、不掃描。
- [ ] `BOM格式` 例外規則維持：有實際 PN／Level 資料時掃描，空白範本不掃描。
- [ ] `【異常檢測報告】` 必須產生，且有 `History` 時放在 `History` 正前方；沒有 `History` 時放在最後。
- [ ] 原始分頁、分頁名稱、順序（除上述報告插入位置）、隱藏狀態、公式、合併儲存格、註解、圖片與既有格式必須保留。
- [ ] 只有系統修改或標記的儲存格套用規定樣式；新增報告分頁全面套用統一樣式。

## 3. 版本與頁面同步

每次可見功能或修正上線時，至少檢查下列位置：

- [ ] `index.html`：頁面 `<title>`。
- [ ] `index.html`：左側 Logo 版本。
- [ ] `index.html`：測試清單版本。
- [ ] `index.html`：側欄底部版本與日期。
- [ ] `index.html`：右上角版本。
- [ ] `index.html`：首頁「最新更新」版本、日期與本次內容。
- [ ] `index.html`：將上一版內容保留到「歷史版本紀錄」，不得直接覆蓋消失。
- [ ] `index.html`：頁面標題對照表中的測試清單版本。
- [ ] `modules/bom-validator/index.html`：BOM 頁面顯示版本。
- [ ] `modules/bom-validator/bom-guide.js`：SOP 共用版本與更新日期。
- [ ] `modules/bom-validator/sop.html`：引用的 SOP 快取版本。
- [ ] 修改過的 JS／CSS 引用要更新 query-string cache buster，避免正式站仍載入舊檔。

## 4. 首頁更新小卡寫法

- [ ] 只寫本次實際完成並已驗證的內容。
- [ ] 使用 `NEW`、`FIX`、`SAFE`、`GUIDE`、`UX` 等清楚標籤。
- [ ] 不宣稱尚未啟用的功能已可使用。
- [ ] 回饋功能只有在「已能寫入 Google Sheet 且成功寄信」後，才能寫成已上線。
- [ ] 正式站尚未完成驗收時，不得寫成已完成正式部署。

## 5. 必要測試

- [ ] 執行 JavaScript 語法與 `git diff --check`。
- [ ] 至少執行既有 Tiny8、Adapter cage、防塵罩與 M90a 真實案例回歸。
- [ ] 測試結果必須為 `0 FAIL`、`0 unreadable`。
- [ ] 驗證所有原始分頁、順序、狀態、公式、合併、註解與圖片保留。
- [ ] 驗證簡體轉繁體沒有殘留既知案例。
- [ ] 驗證異常報告存在、樣式完整且位置正確。
- [ ] 驗證 Defined Names／Print Area 原樣保留。
- [ ] 驗證 `workbook.xml` 沒有 `<calcPr:` 等損壞內容。
- [ ] 使用獨立 Excel 解析器重新開啟輸出檔；不得只用同一套 Web 相容處理重新讀取。
- [ ] 對造成問題的使用者原檔建立或更新回歸案例，避免同類錯誤再次上線。

## 6. 發布前 Git 檢查

- [ ] 顯示 `git status`、未暫存 diff 與已暫存 diff。
- [ ] 只 `git add -- <本次確認檔案>`；禁止 `git add .`、`git add -A`。
- [ ] 不加入內部覆核文件、測試輸出、真實 BOM 或其他不相關檔案。
- [ ] commit 訊息清楚描述本次修正。
- [ ] GitHub stage、commit、push、PR、merge 依規範取得使用者明確授權。

## 7. 部署與線上驗收

- [ ] GitHub PR 合併至正確 base branch。
- [ ] GitHub Pages build 與 deploy 成功。
- [ ] Cloudflare 自動部署若因 `CLOUDFLARE_API_TOKEN` 缺失而失敗，必須明確回報；不得假裝成功。
- [ ] 若使用既有本機 OAuth 直接更新 Cloudflare 預覽站，先 dry-run，再正式 deploy。
- [ ] 正式 GitHub Pages 與 Cloudflare 預覽站均回傳 HTTP 200。
- [ ] 線上頁面版本、cache buster 與實際 JS 修正內容一致。
- [ ] 使用真實回歸檔做線上下載抽驗，確認 Excel 可開啟且報告存在。
- [ ] 首頁最新更新小卡及歷史紀錄在線上可見。

## 8. 更新通知

- [ ] HTML 正式更新後，確認 GitHub 通知工作是否成功。
- [ ] 實際通知名單以 `.github/workflows/notify.yml` 的授權設定為準；公開文件不重複列出個人公司信箱。
- [ ] 不得因通知 Action 成功就宣稱對方已實際開信或閱讀。

## 9. 問題回饋功能啟用條件

- [ ] 使用者必須主動勾選同意後才能送出。
- [ ] 送出內容僅限結構化欄位，不含 Excel 或 BOM 原文。
- [ ] 成功寫入 `BOM Feedback` Google Sheet。
- [ ] 成功寄送通知至 `may_chen@avc.co`。
- [ ] 網頁顯示送出成功／失敗，不得無法確認結果卻顯示成功。
- [ ] Apps Script 尚未部署或網址未設定時，正式頁面不得顯示可用的「送出回饋」。
- [ ] 保留「複製回饋內容／下載回饋紀錄」作為備援。

## 10. 完成回報

- [ ] 清楚列出版本、commit、PR、正式網址與預覽網址。
- [ ] 清楚列出測試數量及 PASS／FAIL。
- [ ] 清楚區分「已完成」、「尚未啟用」及「外部阻擋」。
- [ ] 若刪除或覆蓋任何重要資料，說明影響與是否可復原。
- [ ] 確認下一階段工作，不把尚未完成的項目包裝成已完成。
